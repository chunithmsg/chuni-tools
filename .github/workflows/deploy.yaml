name: Deploy

on:
  # push:
  #   branches:
  #     - main
  workflow_dispatch:

jobs:
  # lint:
  #   name: lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: setup nodejs, pnpm and installs packages
  #       uses: ./.github/actions/setup

  #     - uses: pnpm run lint

  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup nodejs, pnpm and installs packages
        uses: ./.github/actions/setup

      - uses: pnpm run build:apps

  # test:
  #   name: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: setup nodejs, pnpm and installs packages
  #       uses: ./.github/actions/setup

  #     - uses: pnpm run test

  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v4

      - name: deploy
        run: echo "Deploying to production"
        